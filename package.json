{
  "name": "xmtp-admin-ui",
  "version": "1.0.0",
  "description": "Read-only admin dashboard for XMTP smart contracts",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prettier": "prettier --write 'src/**/*.js' 'api/**/*.js' 'index.html'",
    "prettier:check": "prettier --check 'src/**/*.js' 'api/**/*.js' 'index.html'"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "vite": "^7.3.1"
  }
}
